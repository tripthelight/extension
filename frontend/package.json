{
  "name": "extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:watch": "vite build --watch",
    "reset": "rmdir /s /q dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.40",
    "dotenv": "^17.4.1",
    "sass": "^1.99.0",
    "vite": "^8.0.4"
  }
}
